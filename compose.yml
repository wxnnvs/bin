services:
  bin:
    build: .
    container_name: minimal-bin
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - ./pastes:/app/pastes
    restart: unless-stopped
